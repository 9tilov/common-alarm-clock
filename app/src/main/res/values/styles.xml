<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/background_title</item>
        <item name="colorPrimaryDark">@color/background_title</item>
        <item name="colorAccent">@color/background_title</item>
        <item name="android:textColor">@color/background_title</item>
        <item name="android:textColorPrimary">@color/background_title</item>
        <item name="android:editTextStyle">@style/EditTextAppTheme</item>
        <item name="android:checkboxStyle">@style/CheckBoxAppTheme</item>
        <item name="android:radioButtonStyle">@style/RadioButtonAppTheme</item>
        <item name="android:buttonStyle">@style/ButtonAppTheme</item>
        <item name="android:timePickerDialogTheme" tools:targetApi="lollipop">@style/AppThemeTimePicker</item>
    </style>
</resources>
